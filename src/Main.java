import java.io.*;
import javax.swing.*;

public class Main {
    public static void Main(String[] args) {
        JFrame window = new JFrame("Viewed Alpha");
        window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // make sure when close button is hit process closes
        window.setSize(500, 600);
        window.setVisible(true);
    }
}